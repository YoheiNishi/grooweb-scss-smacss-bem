=========================================
** GROOWEB SCSS SMACSS BEM
** セレクタ命名について
=========================================
class値（CSSセレクタ名）は、BEM記法で付与する。
基本的に、単語は略さない

NG例：
container -> cont ×
text -> txt ×
wrapper -> wrap ×
